# OpenAI-compatible API

The canonical guide has moved to
[OpenAI-compatible API](features/openai-compatible-api.md).

This path is retained so existing links and bookmarks continue to work. The
feature guide documents enablement, authentication, supported Chat Completions
behavior, durable conversation mapping, Open WebUI and SDK configuration,
unsupported request shapes, failure behavior, and qualification boundaries.
