<!-- GENERATED by cg specgen from spec/ kvx — DO NOT EDIT. Edit the .kvx source and regenerate. -->

# Implementation Plan

## Tasks

  - [x] 1.1 Protected surface, change classification, and guard integrity
    - Create the keith-self-evolution domain crate with a compiled ProtectedSurface constant covering the agent loop, memory, guard, ledger, and release verification
    - Implement path resolution that evaluates post-resolution paths and rejects absolute, traversal, symlink-escape, device, and rename-into-protected targets
    - Implement ChangeClass A through D classification from resolved paths and artifact manifests, with recomputation and escalation detection
    - Implement the consent policy mapping class to autonomous, watchdog-bounded, human-approved, and refused
    - Test each class boundary, escalation after verification, class D refusal, and that no configuration or model output can widen the surface
    - _Requirements: 2.1-2.6, 3.1-3.7_
  - [x] 1.2 Append-only signed evolution ledger
    - Implement EvolutionLedger records for hypothesis, admission, proposal, gate, canary, consent, promotion, observation, revert, enable, and disable events
    - Sign and verify records through the existing release verification path and enforce append-only semantics with corruption quarantine
    - Bound and redact gate output and exclude secrets, prompts, memory, and private reasoning from every record
    - Test append-only behavior, signature verification, corruption quarantine, redaction under failure paths, and restart continuity
    - _Requirements: 12.1-12.3, 12.5_
  - [x] 1.3 Enablement policy, availability probing, and disable semantics
    - Add a default-off installation-level enablement layer with disclosure, acting identity, and ledger recording
    - Probe toolchain, sandbox, and image-directory availability and report the feature unavailable when verification is impossible
    - Implement immediate disable that aborts in-flight work, discards staged artifacts, stops evidence collection, and preserves reversibility
    - Reject enablement and surface widening from clients, channels, kernels, plugins, MCP, skills, and model output
    - Test default-off behavior with no compiler invocation, fail-closed enablement, and disable during build and canary
    - _Requirements: 1.1-1.6_
  - [x] 2.1 Evidence intake and falsifiable hypothesis lifecycle
    - Implement EvolutionHypothesis with evidence references, target, metric, baseline, target threshold, measurement slice, revert threshold, and expiry
    - Source evidence only from tool experience, telemetry, failure categories, crash reconciliation, and explicit user reports
    - Reject hypotheses lacking a metric, baseline, revert threshold, or resolvable slice, and suppress duplicates and recent failures
    - Implement the durable state machine with validated transitions and restart continuity
    - Test admission rejection, duplicate suppression, expiry, restart continuity, and evidence-content injection resistance
    - _Requirements: 4.1-4.6_
  - [x] 2.2 Shadow source tree and read-only bounded proposal
    - Implement ShadowTree staging at a recorded base revision with full isolation from the live checkout
    - Build the read-only reviewer bundle with no shell, write, network, or credential authority
    - Enforce changed-file, byte, and dependency-addition limits and record exact prior bytes for every changed and deleted path
    - Implement reclamation on completion, abort, disable, and restart
    - Test live-tree isolation, reviewer authority limits, limit enforcement, dependency denial, injection resistance, and reclamation after crash
    - _Requirements: 5.1-5.6_
  - [ ] 2.3 Evaluation corpus and deterministic trace replay harness — **Implemented - qualification pending**
    - Build a versioned evaluation corpus covering direct, planned, delegated, reviewed, tool-using, recovery, and failure journeys with recorded baselines
    - Implement deterministic replay of recorded provider streams, tool results, clock, and randomness
    - Detect and report non-determinism as inconclusive rather than averaging it
    - Keep the corpus free of credentials, personal memory, and private content and make the harness usable for ordinary regression testing
    - Test deterministic replay, non-determinism detection, corpus versioning, baseline comparison, and private-content absence
    - _Requirements: 14.1-14.6_
  - [ ] 3.1 Sandboxed build and complete verification gate — **Implemented - qualification pending**
    - Run builds in a separate sandboxed process with minimal environment, no credentials, and time, CPU, memory, output, process-tree, filesystem, and network limits
    - Execute formatting, strict linting, the real workspace test suite, dependency policy, security gate, and platform gate and derive BuildVerdict only from real exit statuses
    - Make gate failure non-overridable and subject proposal-introduced test code to the same sandbox and secret scanning
    - Produce a signed candidate WorkerImage recording build identifier, source manifest digest, toolchain identity, and gate results
    - Test sandbox containment, gate failure propagation, unsigned and tampered artifact rejection, build timeout cleanup, and gate-weakening refusal
    - _Requirements: 6.1-6.6_
  - [ ] 3.2 Versioned worker image registry and supervisor image resolution — **Implemented - qualification pending**
    - Replace the supervisor's fixed executable with a WorkerImage registry holding current and pinned previous-known-good pointers plus verified metadata
    - Resolve the image at spawn time so each generation records the exact image it runs
    - Verify signature, protected surface, and change class against the artifact manifest immediately before installing an image
    - Implement retention of previous images and reclamation of superseded ones
    - Test image resolution per generation, pre-install verification, tampered image rejection, and retention behavior
    - _Requirements: 8.1-8.2, 11.3_
  - [ ] 4.1 Canary worker evaluation on real supervisor generations — **Implemented - qualification pending**
    - Start a real canary worker on a new generation against a canary root tree using existing lease, registration, control, and drain machinery
    - Run the evaluation corpus and trace replay and report the hypothesis metric, baseline comparison, latency, cost, and failure behavior
    - Isolate the canary from live session trees, channels, delivery destinations, credentials, and destructive tools
    - End the transaction without promotion on crash, startup timeout, lease failure, protocol mismatch, or unmet threshold
    - Reclaim canary resources on every outcome including daemon restart, and prove isolation and reclamation with real processes
    - _Requirements: 7.1-7.6_
  - [ ] 4.2 Live worker roll promotion and source write-back — **Implemented - qualification pending**
    - Install the verified image and roll every active root tree through drain-and-replace onto a new generation without restarting the daemon
    - Return an unhealthy tree to the pinned image and abort the roll with full restoration past a configured failure count
    - Preserve in-flight actions, goals, children, waits, commitments, schedules, and deliveries across the roll with no history loss or duplication
    - Copy verified source changes back into the live tree only after a successful roll, as one transaction recording exact prior bytes
    - Prove a live roll with active sessions, partial roll failure restoration, daemon survival, generation correctness, and crash at every promotion boundary
    - _Requirements: 8.3-8.7_
  - [ ] 4.3 Staged daemon replacement and supervised restart recovery — **Implemented - qualification pending**
    - Stage daemon-affecting changes as verified signed pinned images awaiting a supervised restart instead of rolling live
    - Require class C consent with disclosure of the restart requirement, affected scope, and reversal path
    - Run migration, catalog rebuild, and endpoint opening under the new image with automatic restoration of the previous image on failure
    - Record and surface a triggered restoration on next attach and keep the staged image inspectable
    - Prove staged replacement, successful restart, failed startup and migration restoration, and repeated restart stability with real processes
    - _Requirements: 9.1-9.5_
  - [ ] 5.1 One-click reversal and baseline restore — **Implemented - qualification pending**
    - Implement a single reversal action per promotion that restores the pinned image, rolls workers back, and restores recorded prior source bytes in one transaction
    - Make reversal depend only on recorded bytes and pinned artifacts, with no rebuild, gate, toolchain, or network requirement
    - Implement baseline restore that reverts every self-evolution change to the last human-approved baseline in one action and reports exactly what it reverted
    - Support reversal while disabled, during a build or canary, after restart, and for stacked promotions touching the same paths
    - Guarantee byte-exact restoration or an explicit unresolved state on the pinned known-good image, never a mixed image and source tree
    - Record reversal in the ledger without deleting or rewriting the reverted change's history
    - Prove one-action reversal, stacked and out-of-order reversal, baseline restore, reversal of an unbuildable change, reversal while disabled, and crash at every reversal boundary
    - _Requirements: 10.1-10.7, 12.6_
  - [ ] 5.2 Automatic revert watchdog and known-good advancement — **Implemented - qualification pending**
    - Start a bounded observation window per promotion measuring the hypothesis metric plus crash rate, turn failure rate, latency, cost, and resource use
    - Revert automatically on threshold breach or attributable worker crash loop and notify per profile policy
    - Advance the known-good baseline on a clean window and retain the previous image for the configured period
    - Survive daemon and worker restart, resume the remaining window, and revert on inconclusive measurement
    - Enforce a single active observation window and block new promotions while one is open
    - Test automatic revert on breach, crash loop, and inconclusive measurement, window resumption after restart, and baseline advancement
    - _Requirements: 11.1-11.6_
  - [ ] 5.3 Evolution budgets, anti-goals, and concurrency bounds — **Implemented - qualification pending**
    - Enforce ceilings on concurrent hypotheses, shadow trees, builds, canaries, promotions per interval, and retained images with full resource accounting
    - Run evolution work at background priority through the ordinary action inbox and suppress it before interactive, scheduled, or delivery work
    - Refuse recursive evolution actions, hypotheses, and dedicated children at admission
    - Refuse replication, additional daemons, installation or update channel changes, budget self-edits, and any permission, sandbox, credential, or network widening
    - Abort cleanly on budget exhaustion with full reclamation and no partial artifacts, orphaned processes, or mixed state
    - Prove ceiling enforcement, interactive priority, recursion refusal, replication and widening refusal, and clean abort under load
    - _Requirements: 13.1-13.6_
  - [ ] 6.1 Protocol commands and client evolution surfaces — **Implemented - qualification pending**
    - Add versioned protocol commands, snapshots, and events for enablement, hypothesis and transaction state, consent decisions, ledger browsing, reversal, and baseline restore
    - Build the terminal surface presenting the ledger, readable diff, evidence, measured result, and a single reversal action
    - Build the web surface with the same content in ordinary language, keeping raw identifiers out of primary labels
    - Require authenticated, exact-origin, CSRF-proofed mutations for consent, reversal, enablement, and baseline restore
    - Prove parity of enable, disable, approve, revert, and restore across both clients against real commands
    - _Requirements: 1.2, 1.5, 3.4, 10.1, 10.3, 12.4_
  - [ ] 6.2 Evolution export, deletion, and data-control integration — **Implemented - qualification pending**
    - Add ledger, image, shadow, and corpus scopes to export and deletion with exact scope statements
    - Clean derived projections and previews on deletion and report remaining data on partial failure
    - Keep exports documented, versioned, and readable without a running installation
    - Test export, deletion, remnant scanning, and profile isolation for every evolution scope
    - _Requirements: 12.6_
  - [-] 7.1 Adversarial self-evolution security and fault-injection gate
    - Prove protected-surface evasion is impossible through traversal, symlinks, renames, build scripts, procedural macros, generated code, manifest edits, and toolchain overrides
    - Prove build sandbox containment, network egress denial, credential exfiltration denial across tests, artifacts, ledger, gate output, and diagnostics, and unsigned or tampered promotion refusal
    - Prove prompt injection through source, comments, evidence, corpus data, and failure text cannot change classification, consent, guard state, or promotion
    - Kill the daemon, build, canary, and roll at every durable boundary and prove the live tree, installed image, pinned image, and ledger stay consistent and reversible
    - Run the gate against packaged binaries, a real toolchain, real sandboxes, and real worker processes and block release while any listed issue is open
    - _Requirements: 15.1-15.6, 2.6, 6.6_
  - [ ] 7.2 Self-evolution release qualification and completion gate
    - Run formatting, strict Clippy, dependency policy, the full self-evolution suite, the security gate, and the platform gate
    - Run one end-to-end journey from real evidence through hypothesis, proposal, sandboxed build, canary, promotion, observation, and one-click reversal on real processes
    - Verify default-off packaging, honest unavailability without a toolchain, and that no packaged default invokes a compiler
    - Trace every task to its symbols, touched files, Codify snapshots, commits, and pushed branch
    - Mark the feature complete only when every requirement is evidenced and no unreversible state, unsigned promotion, or fabricated verification result remains
    - _Requirements: 1.1-1.6, 2.1-2.6, 3.1-3.7, 4.1-4.6, 5.1-5.6, 6.1-6.6, 7.1-7.6, 8.1-8.7, 9.1-9.5, 10.1-10.7, 11.1-11.6, 12.1-12.7, 13.1-13.6, 14.1-14.6, 15.1-15.6_

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 1,  "tasks": ["1.1", "1.2", "1.3"] },
    { "id": 2,  "tasks": ["2.1", "2.2", "2.3"] },
    { "id": 3,  "tasks": ["3.1", "3.2"] },
    { "id": 4,  "tasks": ["4.1", "4.2", "4.3"] },
    { "id": 5,  "tasks": ["5.1", "5.2", "5.3"] },
    { "id": 6,  "tasks": ["6.1", "6.2"] },
    { "id": 7,  "tasks": ["7.1"] },
    { "id": 8,  "tasks": ["7.2"] }
  ]
}
```
